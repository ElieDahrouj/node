doctype html
html
    head
        title Page #{name}
        meta(charset='utf-8')
        link(rel='stylesheet', href='https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css')
    body
        div.container.mt-5.d-flex.flex-column
            p   Fiche utilisateur de
                b.ml-1 #{allUser.name}
            p   Email
                b.ml-1 #{allUser.email}
                form(method='POST' action='/user/delete/'+allUser._id)
                    button.btn.btn-danger(type='submit') Delete User
            a(href='/user')
                button.btn.btn-dark Retour
